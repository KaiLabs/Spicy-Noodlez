<li id="event-<%= event.id %>">
  <span class="user"> <%=link_to event.title, event_path(event) %></span>
  <div class="content">Content: <%= event.title %></div>
  <div class="timestamp">
    Posted <%= time_ago_in_words(event.created_at) %> ago.
  </div>
</li>

