<li id="ride-<%= ride.id %>">
  <span class="user"> <%=link_to "Check it out", ride %></span>
  <div class="content">Content: <%= ride.title %></div>
  <div class="timestamp">
    Posted <%= time_ago_in_words(ride.created_at) %> ago.
  </div>
</li>

